<!DOCTYPE html>
<html lang="ru" xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org">

<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title>Users</title>
</head>
<body>
<div align="center">
    <h2>Users Manager</h2>
    <h3><a href="/admin/new">New User</a></h3>
    <table border="1" cellpadding="5">
        <tr>
            <th width="150">ID</th>
            <th width="150">Username</th>
            <th width="150">Name</th>
            <th width="150">E-mail</th>
            <th width="150">Roles</th>
            <th width="150">Password</th>
            <th width="100">Edit</th>
            <th width="100">Delete</th>

        </tr>
        <th:block th:each="msg : ${users}">
            <tr>
                <td th:text="${msg.getId()}"></td>
                <td th:text="${msg.getUsername()}"></td>
                <td th:text="${msg.getName()}"></td>
                <td th:text="${msg.getEmail()}"></td>
                <td th:text="${msg.getRoles()}"></td>
                <td th:text="${msg.getPassword()}"></td>
                <td><a th:href="@{/admin/edit/{id}(id=${msg.getId()})}">Edit</a></td>
                <td>
                    <form th:method="DELETE" th:action="@{/admin/{id}(id=${msg.getId()})}">
                        <input type="submit" value="Delete"/>
                    </form>
                </td>
            </tr>
        </th:block>
    </table>
</div>

<br>
<br>
<a th:href="@{/logout}">Logout</a>

</body>
</html>